[Service]
# The packaged unit bind-mounts /dev/log into haproxy's chroot, which needs
# mount namespacing this container cannot do. Logs go to stdout and the journal
# instead, and the runtime directory has to be declared since nothing else
# creates it for the admin socket.
BindReadOnlyPaths=
RuntimeDirectory=haproxy
